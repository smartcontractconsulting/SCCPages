<!DOCTYPE html>
	<head>
		<meta charset="UTF-8">
		<title>{{.Title}}</title>
	</head>
	<body>
		<div id="header">{{.Header}}</div>
		{{range .Items}}<div id="main">{{ . }}</div>
		<div id="footer">{{.Footer}}</div>
	</body>
</html>
